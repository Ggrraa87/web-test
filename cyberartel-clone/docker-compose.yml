version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cyberartel-website
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - cyberartel-network

networks:
  cyberartel-network:
    driver: bridge
