version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: itsales-website
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - itsales-network

networks:
  itsales-network:
    driver: bridge
